---
'@solana/kit-plugin-rpc': minor
---

Add a `solanaTestnetRpc` bundle plugin that defaults to the public testnet endpoint (`https://api.testnet.solana.com`) and includes airdrop support, mirroring `solanaDevnetRpc`.
