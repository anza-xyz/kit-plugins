---
'@solana/kit-plugins': patch
---

Deprecate the `@solana/kit-plugins` umbrella package. Install individual plugin packages directly instead (e.g. `@solana/kit-plugin-rpc`, `@solana/kit-plugin-payer`, etc.).
