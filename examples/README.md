# Examples

## One-time setup

Start by installing all dependencies.

```shell
pnpm install
```

These examples use a local test Solana validator. They will run on any `solana-test-validator` instance running on `localhost:8899`. No accounts are required.

## Run an example

In the directory of a given example, run its start script.

```shell
pnpm start
```
