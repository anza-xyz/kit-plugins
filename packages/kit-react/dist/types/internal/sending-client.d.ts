import type { ClientWithTransactionSending } from '@solana/kit';
/**
 * @internal
 * Returns the client and asserts it has `sendTransaction` / `sendTransactions`.
 */
export declare function useSendingClient(hookName: string): ClientWithTransactionSending;
//# sourceMappingURL=sending-client.d.ts.map